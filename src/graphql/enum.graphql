enum SortDirection {
  ASC
  DESC
}

enum ItemSortableField {
  id
  name
  brand
  originalPrice
  currentPrice
  score
  itemType
}

enum PostSortableField {
  title
  time
  viewCount
}

enum ItemType {
  TSHIRT
}

enum RecommendPostType {
  RECOMMEND
  OOTD
}

enum RecommendReason {
  DESIGN
  PRICE
}

enum StyleType {
  MINIMALISM
  ETC
}

enum PostType {
  RECOMMEND
  COMMUNITY
}

enum FollowType {
  ITEM
  CHANNEL
  RECOMMENDPOST
}
