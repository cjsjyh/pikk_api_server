enum SortDirection {
  ASC
  DESC
}

enum ItemSortableField {
  id
  name
  brand
  originalPrice
  currentPrice
  score
  itemType
}

enum PostSortableField {
  title
  time
  viewCount
}

enum ItemType {
  TSHIRT
  BLUEJEANS
}
