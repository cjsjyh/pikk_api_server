scalar Upload

type Query {
  helloWorld: String!
}

type Mutation {
  """
  Return > [true] : DB save successful  [false] : DB save unsuccessful
  """
  createUser(userInfo: UserInfo!): Boolean!

  """
  Return >  [true] : DB save successful  [false] : DB save unsuccessful
  """
  createItem(itemInfo: ItemInfo!): Boolean!

  """
  Return > [true] : DB save successful [false] : DB save unsuccessful
  """
  createCommunityArticle(postInfo: CommunityPostInfo!): Boolean!

  """
  postTag and styleTag have to be exactly the same as ENUM saved in DB(case Sensitive)

  Return > [true] : DB save successful  [false] : DB save unsuccessful
  """
  createRecommendArticle(postInfo: RecommendPostInfo!): Boolean!

  """
  TargetType > ["CommunityPost"] ["RecommendPost"]

  Return > [true] : DB save successful  [false] : DB save unsuccessful
  """
  createComment(commentInfo: CommentInfo!): Boolean!

  """
  Toggles Account's Follow status on Channel

  TargetType > ["CommunityPost"] ["RecommendPost"] ["Channel"] ["ITEM"]

  Return > [-1] : Error [0] : Unfollowed [1] : Followed
  """
  FollowTarget(followInfo: FollowInfo!): Int!
}

type Subscription {
  helloWorld: String!
}
