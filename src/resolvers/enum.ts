export enum ItemType {
  TSHIRT,
  BLUEJEANS
}

export enum SortableField {
  id,
  name,
  brand,
  originalPrice,
  currentPrice,
  score,
  itemType
}

export enum SortDirection {
  ASC,
  DESC
}
