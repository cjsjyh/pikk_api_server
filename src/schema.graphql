scalar Upload

type UserCredential {
    username: String!
    password: String!
}

type Query {
  helloWorld: String!
}

type Mutation {
  helloWorld: String!
  
  RegisterUser(username:String!, password:String!, name: String!, email:String!, 
              age:Int!, height:Int!, weight:Int!, profileImg:Upload, phoneNum:String, address:String): Boolean!

  RegisterItem(name: String!, brand: String!, originalPrice: Int!, 
              currentPrice: Int, itemType: String!, itemImage: Upload): Boolean!

  PostChannelPost(accountId: Int!, channelId: Int!, title: String!, content: String!): Boolean!
}

type Subscription {
  helloWorld: String!
}
