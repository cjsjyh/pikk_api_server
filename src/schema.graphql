scalar Upload

type UserCredential {
    username: String!
    password: String!
}

type Query {
  helloWorld: String!
}

type Mutation {
  helloWorld: String!
  RegisterUser(username:String! password:String!, name: String!, email:String!, age:Int!, height:Int!, weight:Int!, profileImg:Upload, phoneNum:String, address:String): Boolean!
}

type Subscription {
  helloWorld: String!
}
